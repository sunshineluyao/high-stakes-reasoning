# Local split outputs

Generate development, validation, and test CSV files locally with
`python -m src.data_split`. They are excluded from version control because they
contain restricted DAIC-WOZ participant identifiers and labels.
